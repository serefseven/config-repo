spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/svn
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: none
      show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

upload-files:
  local=true
  upload-dir=d:/uploads/
